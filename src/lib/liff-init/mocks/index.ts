import { getProfile } from './getProfile';
import { mockConfig } from './config';

// 他のモックも必要に応じてここにインポート/エクスポートする
// 例: import { scanCode } from './scanCode';

export {
  getProfile,
  mockConfig
}
